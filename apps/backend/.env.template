# Runtime
NODE_ENV=development
PORT=9000

# Database
DATABASE_URL=postgres://postgres:postgres@localhost:5432/medusa_garderoba
DB_NAME=medusa_garderoba

# Optional cache / event infra
REDIS_URL=redis://localhost:6379

# CORS
# Local development:
STORE_CORS=http://localhost:8000
ADMIN_CORS=http://localhost:9000,http://localhost:5173
AUTH_CORS=http://localhost:9000,http://localhost:5173,http://localhost:8000

# Production example:
# STORE_CORS=https://your-storefront-domain.rs
# ADMIN_CORS=https://your-backend-domain.onrender.com
# AUTH_CORS=https://your-storefront-domain.rs,https://your-backend-domain.onrender.com

# Secrets
# Generate real production values with:
# openssl rand -base64 32
JWT_SECRET=replace-with-secure-jwt-secret
COOKIE_SECRET=replace-with-secure-cookie-secret
